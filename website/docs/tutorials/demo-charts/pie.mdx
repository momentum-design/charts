---
sidebar_position: 110
---

# Pie Chart

Pie chart is probably the most commonly used charts. They are divided into segments, the arc of each segment shows the proportional value of each piece of data.

They are excellent at showing the relational proportions between data.

```jsx live
function showComponent() {
  const data = {
    'Brunei Darussalam': 739,
    Jordan: 763,
    'Burkina Faso': 604,
    'Cook Islands': 509,
    Monaco: 205,
    Switzerland: 108,
  };
  const options = {};

  return (
    <mdw-chart
      type="pie"
      style={{ width: '300px' }}
      data={JSON.stringify(data)}
      options={JSON.stringify(options)}
    ></mdw-chart>
  );
}
```

## Nested Pie Chart

```jsx live
function showComponent() {
  const data = [
    ['year', 'Good', 'Poor'],
    ['2023', '3432', '200'],
    ['2022', '2565', '500'],
  ];
  const options = {
    dataKey: 'year',
    colors: ['#00cf64', '#d4371c'],
  };

  return (
    <mdw-chart
      type="pie"
      style={{ width: '300px' }}
      data={JSON.stringify(data)}
      options={JSON.stringify(options)}
    ></mdw-chart>
  );
}
```

## Events for Legend

```jsx live noInline
const data = {
  'Brunei Darussalam': 739,
  Jordan: 763,
  'Burkina Faso': 604,
  'Cook Islands': 509,
  Monaco: 205,
  Switzerland: 108,
};
const options = {
  legend: {
    selectable: true,
  },
};

const Example = () => {
  const myRef = createRefWithListener('legendItemClick', (e) => e.context.chart.legend.selectedItems);
  return (<WithActions>
    <mdw-chart
      ref={myRef}
      type="pie"
      style={{ width: '300px' }}
      data={JSON.stringify(data)}
      options={JSON.stringify(options)}
    ></mdw-chart>
  </WithActions>);
}

render(<Example />);
```

## [Options Defaults](doughnut#options-defaults)
