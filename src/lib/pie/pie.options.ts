import { PieOptions } from './pie.types';

const defaultPieChartOptions: PieOptions = {
  legend: {
    display: true,
    position: 'right',
  },
  doughnutLabel: {
    enable: false,
  },
};

export { defaultPieChartOptions };
